import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        sans: ['"Manrope"', "sans-serif"],
      },
    },
  },
  plugins: [],
};

export default config;
